---
name: ixBrowser_qq_publish
description: Use when 用户发出“发视频 1-5”“/发企鹅号 1-5窗口”这类企鹅号发布命令，或只发“发视频”“/发企鹅号”需要补窗口号时。
metadata:
  openclaw:
    emoji: "🐧"
---

# ixBrowser QQ Publish

这是给 agent 用的执行协议。不同宿主可以有自己的适配文件，但命令触发、执行方式、回复方式和停止条件以本文件为准。

## 触发条件

### 1. 发布命令

命令格式：

`[/]发企鹅号|发视频` + 空格 + `窗口号` 或 `起始窗口-结束窗口` + 可选的 `窗口` 后缀

有效示例：

- `发视频 3`
- `发视频 1-10`
- `发企鹅号 3窗口`
- `/发企鹅号 1-10窗口`

不要误触发：

- 命令词和窗口号之间必须有空格
- `发视频1`
- `发视频1-10`
- `发企鹅号3窗口`

### 2. 模式切换命令

命令格式：

`[/]发企鹅号|发视频` + 空格 + `开发模式|正式模式`

示例：

- `发视频 开发模式`
- `/发企鹅号 正式模式`

### 3. 不完整命令

当用户只发以下命令时，不要猜窗口号，也不要直接执行：

- `发企鹅号`
- `发视频`
- `/发企鹅号`
- `/发视频`

直接回复：

`请回复窗口号，例如：1窗口 或 1-5窗口`

## 执行协议

### 收到发布命令后

1. 保留用户原始命令字符串，不要改写窗口范围。
2. 使用当前 skill 安装目录内的 `scripts/run-qq-publisher.ps1`。
3. 在当前 skill 安装目录读取 `config/penguinhao.config.json`，执行前先提示当前模式：
   - `pause-before-publish`：`当前是开发模式，半自动发布，会停在发布按钮前等待人工确认。`
   - `auto-publish`：`当前是正式模式，全自动发布，检查通过后会自动点击发布。`
4. 运行：
   `powershell -ExecutionPolicy Bypass -File .\scripts\run-qq-publisher.ps1 -Command "<用户原命令>"`
5. 读取 CLI 输出的逐窗口中文摘要，直接回给用户，如果某个窗口失败，也要把失败那一行原样包含在回复里。
6. 如果输出是“已完成，停在发布前”，要明确说明该窗口已经停在发布按钮前等待人工确认，不会自动点击发布。
7. 正式模式下某窗口确认发布成功后，CLI 会自动把该窗口使用过的 `.mp4` 移动到 `企鹅号发布/used`；开发模式停在发布前、发布失败或非视频文件不会移动。

### 收到模式切换命令后

1. 只切换模式，不执行发布任务。
2. 直接返回 CLI 的切换结果。
3. “开发模式”表示停在发布前。
4. “正式模式”表示检查通过后自动发布。

### 收到不完整命令后

- 不要猜窗口号。
- 不要直接执行发布。
- 直接回复：
  `请回复窗口号，例如：1窗口 或 1-5窗口`

## 硬约束

### 不要重跑进行中的发布

- 一旦发布命令已经启动，不能因为本地 shell 视图超时、安静了一段时间，或还没拿到最终摘要，就再次启动同一条发布命令。
- 视频上传可能持续 `1-30` 分钟；此时应视为原任务仍在运行，只能继续等待、查看当前终端或日志状态并向用户同步，不能盲目重跑。

### 发布流程必须遵守的顺序

1. 打开发布页并完成登录检查后，先等待 `10` 秒，让旧草稿、标题和封面区域完成异步恢复。
2. 强制清空标题区和正文编辑区，并连续确认正文稳定为空。
3. 填写文章标题后，先处理文章封面和创作声明。
4. 先插入两张正文配图，再把光标移到正文最前方，插入视频。
5. 视频开始上传后，尽早填写视频标题并上传视频封面，不要等视频传完。
6. 最终正文顺序必须是 `视频 -> 配图1 -> 配图2`，并清理多余空行。

对应到具体阶段时，顺序应理解为：

- **文章标题**：在清空旧草稿之后、先填写文章标题。
- **正文配图**：在文章标题之后，先插入两张配图，然后再上传视频。
- **视频标题 / 视频封面**：在视频开始上传后即可设置视频标题和封面，不必等视频上传完。
- **文章封面**：在文章标题之后、正文配图和视频之前处理。
- **创作声明**：在文章封面之后、正文配图和视频之前处理。
- **AI 声明**：在正文配图、视频确认和空行清理之后处理；如果页面已经是确认状态，或没有待处理入口，可以直接跳过。
- **最终复查**：在文章标题、视频、视频封面、文章封面、创作声明、AI 声明都处理完之后统一检查，再决定停在发布前还是继续正式发布。

### 封面判断规则

- 视频封面弹窗里的 tab 是【上传封面】。
- 文章封面弹窗里的 tab 才是【本地上传】。
- 如果正文里已经有插图，或企鹅号恢复了历史草稿素材，文章封面弹窗默认可能先落在【文内图片】；此时要先切回并确认【本地上传】侧，再上传文章封面。
- 视频封面步骤优先按页面当前强信号判断；如果弹窗里已经有可信预览，允许视为封面已就绪继续后续流程。
- 文章封面上传后必须确认封面预览发生变化；如果预览不变，CLI 必须停止并返回 `文章封面上传后预览未变化`。

### 发布前校验规则

- 发布前必须读取页面实际标题值，并与当前视频文件名生成的标题完全一致。
- 标题不一致时先自动改回目标标题并复查；复查仍不一致时停止，不允许发布。
- 正式模式下的发布成功判定优先看发布后页面跳转或管理列表变化，而不只是按钮点击完成。

### 允许继续与必须停止的判断

- 封面、声明和最终复查这类步骤，优先按“能否读到明确状态”判断。
- 能读到目标内容时严格校验。
- 读不到精确内容时，只要页面已经给出足够强的成功信号，就允许继续。
- 只有明确切错 tab、预览长期不变、标题不一致、关键结果既读不到也没有可信替代信号、或出现无法继续的矛盾状态时才停止。

### 长等待时必须持续反馈

- CLI 在长等待阶段要持续输出明确进度。
- 视频上传开始后，应出现类似：
  - `视频上传已开始，通常需要1-30分钟；继续设置视频标题和封面，等待期间不要结束任务`
  - `视频标题和封面已设置，正在等待上传完成；等待期间不要结束任务`
- 如果视频上传仍未结束，要每 `30` 秒继续输出一次进度消息。
- 优先尝试读出弹窗里的百分比，例如：
  `视频上传进度 59.38%，继续等待，不要结束任务`
- 如果弹窗没有百分比，就退回到：
  `视频上传进度 已等待 30 秒，继续等待，不要结束任务`

### 页面落稳与确认按钮规则

- 配图确认、标题回读、封面上传、声明确认、发布按钮等非上传关键步骤后，要短暂停顿，让页面状态先落稳再继续。
- 视频上传弹窗第一次点【确认】后，如果按钮进入动画、loading、disabled 或其他处理中状态，要继续观察，不要因为弹窗还没立刻关闭就过早第二次点击。
- 视频上传开始后，不能为了“重试”而刷新页面或重新启动同一条发布命令。

## 依赖

- 默认依赖 `config/penguinhao.config.json`。
- CLI 会自动读取配置、分配视频、挑封面、打开 ixBrowser profile、连接浏览器、执行发布、移动已发布视频并写日志。

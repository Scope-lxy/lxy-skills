---
name: ixBrowser_qq_publish
description: 当用户发出 `发视频 1-5`、`/发企鹅号 1-5窗口` 这类命令，或先发 `发视频`、`/发企鹅号` 需要补窗口号时，运行本地 CLI 串起 ixBrowser 与企鹅号发布流程，并返回逐窗口中文结果。
metadata:
  {
    "openclaw":
      {
        "emoji": "🐧",
      },
  }
---

# ixBrowser QQ Publish

这是本 skill 的通用真源说明。不同宿主可以额外挂自己的适配文件，但行为规则都以本文件为准。

当用户消息本身符合发布命令格式时，执行本 skill：

`[/]发企鹅号|发视频` + 至少 1 个空格 + `窗口号` 或 `起始窗口-结束窗口` + 可选的 `窗口` 后缀。

有效示例：

- `发视频 3`
- `发视频 1-10`
- `发企鹅号 3窗口`
- `/发企鹅号 1-10窗口`

为避免误触发，命令词和窗口号之间必须有空格。`发视频1`、`发视频1-10`、`发企鹅号3窗口` 都不是有效发布命令。

执行规则：

1. 保留用户原始命令字符串，不要改写窗口范围。
2. 使用当前 skill 安装目录内的 `scripts/run-qq-publisher.ps1`。
3. 在当前 skill 安装目录读取 `config/penguinhao.config.json`，执行发布命令前先向用户提示当前模式：
   - `mode` 为 `pause-before-publish`：`当前是开发模式，半自动发布，会停在发布按钮前等待人工确认。`
   - `mode` 为 `auto-publish`：`当前是正式模式，全自动发布，检查通过后会自动点击发布。`
4. 运行：
   `powershell -ExecutionPolicy Bypass -File .\scripts\run-qq-publisher.ps1 -Command "<用户原命令>"`
   一旦这条发布命令已经启动，不能因为本地 shell 视图超时、安静了一段时间，或还没拿到最终摘要，就再次启动同一条发布命令。视频上传可能持续 `1-30` 分钟；此时应视为原任务仍在运行，只能继续等待、查看当前终端/日志状态并向用户同步，不能盲目重跑。
5. 读取 CLI 输出的逐窗口中文摘要，直接回给用户。
6. 如果某个窗口失败，也要把失败那一行原样包含在回复里。
7. 如果输出是“已完成，停在发布前”，明确说明该窗口已经停在发布按钮前等待人工确认，不会自动点击发布。
8. 正式模式下某窗口确认发布成功后，CLI 会自动把该窗口使用过的 `.mp4` 视频移动到 `企鹅号发布/used`，避免下次重复分配；开发模式停在发布前、发布失败或非视频文件不会移动。
9. 当前正文搭建顺序是：打开发布页并完成登录检查后先等待 `10` 秒，让旧草稿、标题和封面区域完成异步恢复；再强制清空标题区和正文编辑区，并连续确认正文稳定为空；再插入两张正文配图，再把光标移到正文最前插入视频；视频开始上传后会尽早填写视频标题并上传视频封面，不等视频传完才做这两步；最终校验目标仍是 `视频 -> 配图1 -> 配图2`，并会清理多余空行。
10. 发布前会读取页面实际标题值，必须和当前视频文件名生成的标题完全一致；标题不一致时先自动改回目标标题并复查，复查仍不一致才停止，不允许发布。
11. 文章封面上传后必须确认封面预览发生变化；如果预览不变，CLI 会停止并返回 `文章封面上传后预览未变化`，不能继续走到发布前。
12. CLI 在长等待阶段会持续输出进度，例如 `视频上传已开始，通常需要1-30分钟；继续设置视频标题和封面，等待期间不要结束任务`、`视频标题和封面已设置，正在等待上传完成；等待期间不要结束任务`。如果视频上传仍未结束，会每 `60` 秒继续输出一次 `视频上传流程仍在进行，可能持续1-30分钟；继续等待，不要结束任务`，避免外层 agent 因静默误判流程已经结束。
13. 配图确认、标题回读、封面上传、声明确认、发布按钮等非上传关键步骤后会短暂停顿，让页面状态先落稳再继续；但不能在视频上传开始后为了“重试”而刷新或重新启动同一条发布命令。

当用户消息本身符合模式切换格式时：

`[/]发企鹅号|发视频` + 至少 1 个空格 + `开发模式|正式模式`

例如 `发视频 开发模式`、`/发企鹅号 正式模式`。

执行规则：

1. 只切换模式，不执行发布任务。
2. 直接返回 CLI 的切换结果。
3. “开发模式”表示停在发布前。
4. “正式模式”表示检查通过后自动发布。

当用户只发以下不完整命令时：

- `发企鹅号`
- `发视频`
- `/发企鹅号`
- `/发视频`

不要自行猜窗口号，也不要直接执行。应直接提示用户：

`请回复窗口号，例如：1窗口 或 1-5窗口`

运行前默认依赖 `config/penguinhao.config.json`，CLI 会自动读取配置、分配视频、挑封面、打开 ixBrowser profile、连接浏览器、执行发布、移动已发布视频并写日志。

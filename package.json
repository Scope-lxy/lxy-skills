{
  "name": "ixbrowser-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json",
    "qq:publish": "node --import tsx ./src/cli.ts"
  },
  "dependencies": {
    "playwright": "^1.54.2",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
